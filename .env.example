# =============================================================================
# KudiManager - Environment Variables Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control - it contains sensitive data!

# -----------------------------------------------------------------------------
# SECURITY & AUTHENTICATION
# -----------------------------------------------------------------------------
# JWT Secret - REQUIRED for production
# Generate a strong random string: openssl rand -base64 32
SESSION_SECRET=your-super-secret-session-key-change-this-in-production
JWT_SECRET=${SESSION_SECRET}

# -----------------------------------------------------------------------------
# API CONFIGURATION
# -----------------------------------------------------------------------------
# API Base URL (for frontend to connect to backend)
# Development: leave empty (uses same origin)
# Production: https://your-api-domain.com
VITE_API_URL=

# Server Port (default: 5000)
PORT=5000

# Node Environment
NODE_ENV=development

# -----------------------------------------------------------------------------
# FIREBASE CONFIGURATION
# -----------------------------------------------------------------------------
# Firebase Web App Configuration
# Get these from: Firebase Console > Project Settings > Your Apps > Web App
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# -----------------------------------------------------------------------------
# AI INTEGRATION (Google Gemini)
# -----------------------------------------------------------------------------
# Replit AI Integration automatically sets these (use Replit integrations tool)
# If manual setup needed, get API key from: https://makersuite.google.com/app/apikey
AI_INTEGRATIONS_GEMINI_API_KEY=your-gemini-api-key
AI_INTEGRATIONS_GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta

# -----------------------------------------------------------------------------
# PAYMENT GATEWAY (Paystack)
# -----------------------------------------------------------------------------
# Paystack API Keys - REQUIRED for payment processing
# Get from: https://dashboard.paystack.com/#/settings/developer
# Test keys start with sk_test_ and pk_test_
# Live keys start with sk_live_ and pk_live_
PAYSTACK_SECRET=sk_test_your-secret-key-here
VITE_PAYSTACK_PUBLIC_KEY=pk_test_your-public-key-here

# Paystack Callback URL (where users return after payment)
# This MUST be a publicly accessible URL in production
# Example: https://kudimanager.com
PAYSTACK_CALLBACK_URL=

# -----------------------------------------------------------------------------
# REPLIT DEPLOYMENT (Auto-configured)
# -----------------------------------------------------------------------------
# These are automatically set by Replit - DO NOT MODIFY
# REPL_ID=
# REPL_SLUG=
# REPL_OWNER=

# -----------------------------------------------------------------------------
# PRODUCTION CHECKLIST
# -----------------------------------------------------------------------------
# Before deploying to production, ensure:
# ✅ SESSION_SECRET is a strong random string (not the default)
# ✅ VITE_API_URL points to your production API
# ✅ All Firebase keys are from your production Firebase project
# ✅ AI_INTEGRATIONS_GEMINI_API_KEY is set (if using AI features)
# ✅ NODE_ENV=production
# ✅ firestore.rules are deployed: firebase deploy --only firestore:rules
# ✅ CORS is configured for your production domain in api/app.ts
# ✅ PAYSTACK_SECRET is set with your live key (sk_live_...)
# ✅ VITE_PAYSTACK_PUBLIC_KEY is set with your live public key (pk_live_...)
# ✅ PAYSTACK_CALLBACK_URL is set to your production frontend URL
